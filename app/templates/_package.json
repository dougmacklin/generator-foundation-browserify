{
  "name": "<%= props.appName %>",
  "version": "0.0.1",
  "description": "description of <%= props.appName %>",
  "main": "gulpfile.js",
  "scripts": {
    "start": "gulp",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "homepage": "",
  "author": {
    "name": "",
    "email": ""
  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "license": "ISC",
  "devDependencies": {
    "babel-preset-es2015": "^6.3.13",
    "browser-sync": "^2.7.6",
    "browserify": "^10.1.2",
    "browserify-shim": "^3.8.6",
    "gulp": "^3.8.11",<% if (props.autoprefixer) { %>
    "gulp-autoprefixer": "^3.1.0",<% } %><% if (props.babel) { %>
    "gulp-babel": "^6.1.1",<% } %>
    "gulp-clean-css": "^2.2.2",<% if (props.compass) { %>
    "gulp-compass": "^2.0.4",<% } %>
    "gulp-concat": "^2.6.0",<% if (props.pug) { %>
    "gulp-pug": "^3.2.0",<% } %><% if (!props.pug) { %>
    "gulp-minify-html": "^1.0.2",<% } %><% if (!props.compass) { %>
    "gulp-sass": "^2.0.0",<% } %>
    "gulp-sequence": "^0.3.2",
    "gulp-sourcemaps": "^1.6.0",
    "gulp-streamify": "0.0.5",
    "gulp-uglify": "^1.2.0",
    "gulp-util": "^3.0.4",
    "gulp-watch": "^4.2.4",<% if (props.motionUI) { %>
    "motion-ui": "^1.1.1",<% } %><% if (props.bourbon) { %>
    "node-bourbon": "^4.2.3",<% } %>
    "vinyl-buffer": "^1.0.0",
    "vinyl-source-stream": "^1.1.0",
    "watchify": "^3.2.1"
  },
  "dependencies": {
    <% if (props.foundationVersion === 'Foundation 6') { %>"foundation-sites": "^6.2.4"<% } else { %>"foundation-sites": "^5.5.3"<% } %>,
    <% if (props.foundationVersion === 'Foundation 6') { %>"jquery": "^2.1.4"<% } else { %>"jquery": "2.1.4"<% } %>
  },
  "browser": {
    "jquery": "./node_modules/jquery/dist/jquery.js",
    "foundation": "./node_modules/foundation-sites/js/foundation.js"
  }
}
